//Print 1-5, using a for loop unlike previous examples using while loops
for (var count = 1; count < 6; count++) { //note, scope of var count exists within for loop, unlike while where var is declared globally outside of loop
  console.log(count);
}
